# Source registry

`sources.jsonl` is the canonical source registry. It keeps source URLs,
collection titles, repository details, access dates, and citation metadata
where available. Subscription links may require the researcher's signed-in
Ancestry session.
